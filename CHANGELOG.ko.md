# Changelog

[English](CHANGELOG.md)

## [0.1.0] - 2026-09-22

첫 릴리스.

### 추가됨

- 바탕화면에 박히는 월간 캘린더 (DesktopCal 스타일: 배경화면 위, 아이콘 아래)
  - Win11 최신 XAML island 데스크톱 지원 (Progman 위 z-order 고정 + 클릭 통과)
  - 구형 WorkerW 부착 폴백, 자동 감지
- Google 캘린더 읽기 전용 연동 (ICS 비밀 주소) — 다중 피드, 캘린더별 색상
- 종일 일정 색상 칩 표시, 시간 일정 `HH:mm` 표시
- 반복 일정(RRULE) 확장, 시간대 변환, 며칠짜리 일정 (Ical.Net)
- 클릭 통과 창 위에서 동작하는 전역 로우레벨 마우스 훅
- UI Automation 바탕화면 아이콘 판정 (아이콘 클릭 통과)
- 자유 배치 스티커 메모 (셀 더블클릭 생성, 위치 조정 모드에서 이동/크기 조절)
- 우측 메모 패널, 인라인 편집 (더블클릭으로 바로 타이핑, Enter 저장)
- 트레이 상주: 새로고침, Google 캘린더 바로가기, 스티커 추가, 위치 잠금, 설정, 종료
- 설정: ICS URL, 폰트, 테마(다크/라이트/미니멀/웜), 언어(한국어/English),
  모니터 선택, 새로고침 주기, 주 시작 요일, 전체화면/영역 모드,
  크기 배율, 셀/전체 불투명도, 시작 프로그램, 기본값 복원
- Pretendard 폰트 번들 (SIL OFL)
- `settings.json`의 `DebugLogEnabled`로 디버그 로그
