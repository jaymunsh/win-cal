<Window x:Class="WinCal.StickerEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="스티커" Width="340" SizeToContent="Height"
        WindowStartupLocation="CenterScreen" Topmost="True"
        ShowInTaskbar="False" ResizeMode="NoResize"
        Background="#F8F8F8">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText" Grid.Row="0" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,6"/>
        <TextBox x:Name="MemoBox" Grid.Row="1" MinHeight="100" AcceptsReturn="True"
                 TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                 FontSize="13" Padding="6"/>
        <DockPanel Grid.Row="2" Margin="0,12,0,0">
            <Button x:Name="DeleteButton" Width="72" DockPanel.Dock="Left" Click="Delete_Click"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="SaveButton" Width="72" Margin="0,0,8,0" IsDefault="True" Click="Save_Click"/>
                <Button x:Name="CancelButton" Width="72" IsCancel="True" Click="Cancel_Click"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
